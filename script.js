const money = n => new Intl.NumberFormat('th-TH',{style:'currency',currency:'THB',maximumFractionDigits:0}).format(n);
const aov=document.getElementById('aov'),rate=document.getElementById('rate'),conv=document.getElementById('conv');
function calc(){if(!aov)return;document.getElementById('aovOut').textContent=money(+aov.value);document.getElementById('rateOut').textContent=rate.value+'%';document.getElementById('convOut').textContent=Number(conv.value).toLocaleString();const g=+aov.value*+conv.value;document.getElementById('gmv').textContent=money(g);document.getElementById('payout').textContent=money(g*(+rate.value/100))}
[aov,rate,conv].forEach(x=>x&&x.addEventListener('input',calc));calc();
document.querySelectorAll('.filter').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');const f=b.dataset.filter;document.querySelectorAll('.campaign').forEach(c=>c.style.display=(f==='all'||c.dataset.category===f)?'block':'none')}));
const d=document.getElementById('campaignDialog');document.querySelectorAll('.campaign-btn').forEach(b=>b.addEventListener('click',()=>{document.getElementById('dialogTitle').textContent=b.dataset.campaign;d.showModal()}));document.querySelector('.dialog-close')?.addEventListener('click',()=>d.close());document.querySelector('.dialog-join')?.addEventListener('click',()=>d.close());
const role=document.getElementById('role'),goal=document.getElementById('goalLabel');function setRole(r){role.value=r;document.querySelectorAll('.role').forEach(x=>x.classList.toggle('active',x.dataset.role===r));goal.firstChild.textContent=r==='advertiser'?'What outcome do you want to drive?':'Where do you distribute traffic?';goal.querySelector('textarea').placeholder=r==='advertiser'?'Example: drive online sales for a fashion store in Thailand':'Example: Facebook groups, TikTok, niche page, website'}
document.querySelectorAll('.role').forEach(b=>b.addEventListener('click',()=>setRole(b.dataset.role)));document.querySelectorAll('[data-role-link]').forEach(a=>a.addEventListener('click',()=>setRole(a.dataset.roleLink)));
document.getElementById('joinForm')?.addEventListener('submit',e=>{e.preventDefault();document.getElementById('formMessage').textContent='MVP form captured locally. Connect this form to your email, CRM or database before launch.'});